# Core Dependencies for Gradient-Aware RL DVFS Governor
# Exact pins match the environment the committed models/*.zip and
# models/**/*_vecnormalize.pkl were actually trained and saved with; numpy's
# internal module layout changed between 1.x and 2.x (numpy.core ->
# numpy._core), so cloudpickled SB3 model archives saved under numpy 2.x
# cannot be deserialized under numpy<2.0. Pinned exactly (not a range) so
# pip resolves quickly instead of backtracking across the large torch/numpy
# compatibility matrix, which was observed to hang CI for 10+ minutes with
# range-based pins.
numpy==2.5.1
gymnasium==1.3.0
stable-baselines3==2.9.0
torch==2.13.0
matplotlib>=3.7.0
scipy>=1.10.0,<2.0.0
python-docx>=1.2.0
pytest>=7.4.0
